schema: paperclip/v1

agents:
  ceo:
    adapter:
      type: claude_local
      config:
        model: claude-sonnet-4-6
    budget:
      monthlyLimitCents: 10000
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
        GOOGLE_WORKSPACE_CLI_CREDENTIALS_FILE:
          kind: plain
          requirement: optional
          default: /paperclip/.gws/figurio.json
        AGENT_EMAIL:
          kind: plain
          requirement: optional
          default: ceo@cellarwood.org
        COMPANY_DOMAIN:
          kind: plain
          requirement: optional
          default: cellarwood.org

  cto:
    adapter:
      type: claude_local
      config:
        model: claude-sonnet-4-6
    budget:
      monthlyLimitCents: 15000
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
        DOCKER_HUB_USERNAME:
          kind: secret
          requirement: optional
        DOCKER_HUB_TOKEN:
          kind: secret
          requirement: optional

  cmo:
    adapter:
      type: claude_local
      config:
        model: claude-sonnet-4-6
    budget:
      monthlyLimitCents: 10000
    inputs:
      env:
        GEMINI_API_KEY:
          kind: secret
          requirement: optional
        ELEVENLABS_API_KEY:
          kind: secret
          requirement: optional
        MEDIA_OUTPUT_DIR:
          kind: plain
          requirement: optional
          default: /workspace/media-output
        GOOGLE_WORKSPACE_CLI_CREDENTIALS_FILE:
          kind: plain
          requirement: optional
          default: /paperclip/.gws/figurio.json
        AGENT_EMAIL:
          kind: plain
          requirement: optional
          default: cmo@cellarwood.org
        COMPANY_DOMAIN:
          kind: plain
          requirement: optional
          default: cellarwood.org

  backend-engineer:
    adapter:
      type: claude_local
      config:
        model: claude-sonnet-4-6
    budget:
      monthlyLimitCents: 15000
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
        DATABASE_URL:
          kind: secret
          requirement: optional
        STRIPE_SECRET_KEY:
          kind: secret
          requirement: optional
        STRIPE_WEBHOOK_SECRET:
          kind: secret
          requirement: optional

  frontend-engineer:
    adapter:
      type: claude_local
      config:
        model: claude-sonnet-4-6
    budget:
      monthlyLimitCents: 15000
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional

  devops-engineer:
    adapter:
      type: claude_local
      config:
        model: claude-sonnet-4-6
    budget:
      monthlyLimitCents: 10000
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
        DOCKER_HUB_USERNAME:
          kind: secret
          requirement: optional
        DOCKER_HUB_TOKEN:
          kind: secret
          requirement: optional
        SENTRY_DSN:
          kind: plain
          requirement: optional

  head-of-operations:
    adapter:
      type: claude_local
      config:
        model: claude-sonnet-4-6
    budget:
      monthlyLimitCents: 8000
    inputs:
      env:
        STRIPE_SECRET_KEY:
          kind: secret
          requirement: optional
        GOOGLE_WORKSPACE_CLI_CREDENTIALS_FILE:
          kind: plain
          requirement: optional
          default: /paperclip/.gws/figurio.json
        AGENT_EMAIL:
          kind: plain
          requirement: optional
          default: operations@cellarwood.org
        COMPANY_DOMAIN:
          kind: plain
          requirement: optional
          default: cellarwood.org

  content-creator:
    adapter:
      type: claude_local
      config:
        model: claude-sonnet-4-6
    budget:
      monthlyLimitCents: 8000
    inputs:
      env:
        GEMINI_API_KEY:
          kind: secret
          requirement: optional
        ELEVENLABS_API_KEY:
          kind: secret
          requirement: optional
        MEDIA_OUTPUT_DIR:
          kind: plain
          requirement: optional
          default: /workspace/media-output
        GOOGLE_WORKSPACE_CLI_CREDENTIALS_FILE:
          kind: plain
          requirement: optional
          default: /paperclip/.gws/figurio.json
        AGENT_EMAIL:
          kind: plain
          requirement: optional
          default: content@cellarwood.org
        COMPANY_DOMAIN:
          kind: plain
          requirement: optional
          default: cellarwood.org

  ui-designer:
    adapter:
      type: claude_local
      config:
        model: claude-sonnet-4-6
    budget:
      monthlyLimitCents: 8000
    inputs:
      env:
        GEMINI_API_KEY:
          kind: secret
          requirement: optional
        ELEVENLABS_API_KEY:
          kind: secret
          requirement: optional
        MEDIA_OUTPUT_DIR:
          kind: plain
          requirement: optional
          default: /workspace/media-output

routines:
  weekly-strategic-review:
    triggers:
      - kind: schedule
        cronExpression: "0 9 * * 1"
        timezone: Europe/Prague
  weekly-engineering-standup:
    triggers:
      - kind: schedule
        cronExpression: "0 10 * * 1"
        timezone: Europe/Prague
